import type { AppIdentity } from "@solana-mobile/mobile-wallet-adapter-protocol";

/** Shown in the wallet authorization prompt. */
export const MWA_APP_IDENTITY: AppIdentity = {
  name: "SplitSol",
  uri: "https://www.splitsol.app",
};
